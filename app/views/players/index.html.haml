
.row
  %h2.col-md-12
    1v1 Rankings

.row
  .col-md-12
    %h4 Not on this list?
    %h4 Challenge another player to a 1v1 game!

.row
  .players-list.col-md-12
    %table.table.table-striped
      %tr
        %th Rank
        %th Player
        %th Rating

      - @players.each.with_index do |player, index|
        %tr
          %td.rank ##{index + 1}
          %td.player
            .media
              .media-left
                = link_to player do
                  %img.media-object{src: player.image || default_avatar_image_path, width: 64}
              .media-body
                = link_to player do
                  %h4.media-heading #{player.name}
                  %p= player.nickname
          %td.rating
            = elo player.elo_rating_with_bonus


